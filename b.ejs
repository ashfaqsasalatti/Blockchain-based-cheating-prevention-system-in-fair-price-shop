<!DOCTYPE html>
  <html>
    <head>
      <title>Render JSON data in HTML using Node.js and EJS</title>
    </head>
    <body>
      <ul>
        <% for (const item of newI) { %>
          <li><%= item.name %> - <%= item.price * amtt %></li>
        <% } %>
      </ul>
      <button><a href="/distribute">Continue</a></button>
    </body>
  </html>